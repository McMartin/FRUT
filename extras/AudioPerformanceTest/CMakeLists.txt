# This file was generated by Jucer2CMake from AudioPerformanceTest.jucer

cmake_minimum_required(VERSION 3.4)


list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}/../../cmake")
include(Reprojucer)


if(NOT DEFINED AudioPerformanceTest_jucer_FILE)
  message(FATAL_ERROR "AudioPerformanceTest_jucer_FILE must be defined")
endif()

get_filename_component(AudioPerformanceTest_jucer_FILE
  "${AudioPerformanceTest_jucer_FILE}" ABSOLUTE
  BASE_DIR "${CMAKE_BINARY_DIR}"
)

if(NOT EXISTS "${AudioPerformanceTest_jucer_FILE}")
  message(FATAL_ERROR "No such file: ${AudioPerformanceTest_jucer_FILE}")
endif()

get_filename_component(AudioPerformanceTest_jucer_DIR
  "${AudioPerformanceTest_jucer_FILE}" DIRECTORY
)


jucer_project_begin(
  PROJECT_NAME "AudioPerformanceTest"
  PROJECT_VERSION "1.0.0"
  # COMPANY_NAME
  # COMPANY_WEBSITE
  # COMPANY_EMAIL
  PROJECT_TYPE "GUI Application"
  BUNDLE_IDENTIFIER "com.juce.AudioPerformanceTest"
  BINARYDATACPP_SIZE_LIMIT "Default"
  # BINARYDATA_NAMESPACE
  # PREPROCESSOR_DEFINITIONS
  PROJECT_ID "AKfc5m"
)

jucer_project_files("AudioPerformanceTest/Source"
  "${AudioPerformanceTest_jucer_DIR}/Source/Main.cpp"
  "${AudioPerformanceTest_jucer_DIR}/Source/MainComponent.h"
)

jucer_project_module(
  juce_audio_basics
  PATH "${AudioPerformanceTest_jucer_DIR}/../../modules"
)

jucer_project_module(
  juce_audio_devices
  PATH "${AudioPerformanceTest_jucer_DIR}/../../modules"
  # JUCE_ASIO
  # JUCE_WASAPI
  # JUCE_WASAPI_EXCLUSIVE
  # JUCE_DIRECTSOUND
  # JUCE_ALSA
  # JUCE_JACK
  # JUCE_USE_ANDROID_OPENSLES
)

jucer_project_module(
  juce_audio_formats
  PATH "${AudioPerformanceTest_jucer_DIR}/../../modules"
  # JUCE_USE_FLAC
  # JUCE_USE_OGGVORBIS
  # JUCE_USE_MP3AUDIOFORMAT
  # JUCE_USE_LAME_AUDIO_FORMAT
  # JUCE_USE_WINDOWS_MEDIA_FORMAT
)

jucer_project_module(
  juce_audio_processors
  PATH "${AudioPerformanceTest_jucer_DIR}/../../modules"
  # JUCE_PLUGINHOST_VST
  # JUCE_PLUGINHOST_VST3
  # JUCE_PLUGINHOST_AU
)

jucer_project_module(
  juce_audio_utils
  PATH "${AudioPerformanceTest_jucer_DIR}/../../modules"
  # JUCE_USE_CDREADER
  # JUCE_USE_CDBURNER
)

jucer_project_module(
  juce_core
  PATH "${AudioPerformanceTest_jucer_DIR}/../../modules"
  # JUCE_FORCE_DEBUG
  # JUCE_LOG_ASSERTIONS
  # JUCE_CHECK_MEMORY_LEAKS
  # JUCE_DONT_AUTOLINK_TO_WIN32_LIBRARIES
  # JUCE_INCLUDE_ZLIB_CODE
  # JUCE_USE_CURL
  # JUCE_CATCH_UNHANDLED_EXCEPTIONS
  # JUCE_ALLOW_STATIC_NULL_VARIABLES
)

jucer_project_module(
  juce_data_structures
  PATH "${AudioPerformanceTest_jucer_DIR}/../../modules"
)

jucer_project_module(
  juce_events
  PATH "${AudioPerformanceTest_jucer_DIR}/../../modules"
)

jucer_project_module(
  juce_graphics
  PATH "${AudioPerformanceTest_jucer_DIR}/../../modules"
  # JUCE_USE_COREIMAGE_LOADER
  # JUCE_USE_DIRECTWRITE
)

jucer_project_module(
  juce_gui_basics
  PATH "${AudioPerformanceTest_jucer_DIR}/../../modules"
  # JUCE_ENABLE_REPAINT_DEBUGGING
  # JUCE_USE_XSHM
  # JUCE_USE_XRENDER
  # JUCE_USE_XCURSOR
)

jucer_project_module(
  juce_gui_extra
  PATH "${AudioPerformanceTest_jucer_DIR}/../../modules"
  # JUCE_WEB_BROWSER
  # JUCE_ENABLE_LIVE_CONSTANT_EDITOR
)

jucer_project_end()
