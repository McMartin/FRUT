# This file was generated by Jucer2CMake from OSCReceiver.jucer

cmake_minimum_required(VERSION 3.4)


list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}/../../../../../cmake")
include(Reprojucer)


if(NOT DEFINED OSCReceiver_jucer_FILE)
  message(FATAL_ERROR "OSCReceiver_jucer_FILE must be defined")
endif()

get_filename_component(OSCReceiver_jucer_FILE
  "${OSCReceiver_jucer_FILE}" ABSOLUTE
  BASE_DIR "${CMAKE_BINARY_DIR}"
)

if(NOT EXISTS "${OSCReceiver_jucer_FILE}")
  message(FATAL_ERROR "No such file: ${OSCReceiver_jucer_FILE}")
endif()

get_filename_component(OSCReceiver_jucer_DIR
  "${OSCReceiver_jucer_FILE}" DIRECTORY
)


jucer_project_begin(
  PROJECT_FILE "${OSCReceiver_jucer_FILE}"
  PROJECT_ID "pdocPt"
)

jucer_project_settings(
  PROJECT_NAME "OSCReceiver"
  PROJECT_VERSION "1.0.0"
  # COMPANY_NAME
  # COMPANY_WEBSITE
  # COMPANY_EMAIL
  PROJECT_TYPE "GUI Application"
  BUNDLE_IDENTIFIER "com.yourcompany.OSCReceiver"
  BINARYDATACPP_SIZE_LIMIT "Default"
  # BINARYDATA_NAMESPACE
  # PREPROCESSOR_DEFINITIONS
)

jucer_project_files("OSCReceiver/Source"
  "${OSCReceiver_jucer_DIR}/Source/MainComponent.h"
  "${OSCReceiver_jucer_DIR}/Source/Main.cpp"
)

jucer_project_module(
  juce_core
  PATH "${OSCReceiver_jucer_DIR}/../../modules"
  # JUCE_FORCE_DEBUG
  # JUCE_LOG_ASSERTIONS
  # JUCE_CHECK_MEMORY_LEAKS
  # JUCE_DONT_AUTOLINK_TO_WIN32_LIBRARIES
  # JUCE_INCLUDE_ZLIB_CODE
  # JUCE_USE_CURL
  # JUCE_CATCH_UNHANDLED_EXCEPTIONS
  # JUCE_ALLOW_STATIC_NULL_VARIABLES
)

jucer_project_module(
  juce_cryptography
  PATH "${OSCReceiver_jucer_DIR}/../../modules"
)

jucer_project_module(
  juce_data_structures
  PATH "${OSCReceiver_jucer_DIR}/../../modules"
)

jucer_project_module(
  juce_events
  PATH "${OSCReceiver_jucer_DIR}/../../modules"
)

jucer_project_module(
  juce_graphics
  PATH "${OSCReceiver_jucer_DIR}/../../modules"
  # JUCE_USE_COREIMAGE_LOADER
  # JUCE_USE_DIRECTWRITE
)

jucer_project_module(
  juce_gui_basics
  PATH "${OSCReceiver_jucer_DIR}/../../modules"
  # JUCE_ENABLE_REPAINT_DEBUGGING
  # JUCE_USE_XSHM
  # JUCE_USE_XRENDER
  # JUCE_USE_XCURSOR
)

jucer_project_module(
  juce_gui_extra
  PATH "${OSCReceiver_jucer_DIR}/../../modules"
  # JUCE_WEB_BROWSER
  # JUCE_ENABLE_LIVE_CONSTANT_EDITOR
)

jucer_project_module(
  juce_osc
  PATH "${OSCReceiver_jucer_DIR}/../../modules"
)

jucer_export_target(
  "Xcode (MacOSX)"
  # EXTRA_PREPROCESSOR_DEFINITIONS
  # EXTRA_COMPILER_FLAGS
)

jucer_export_target_configuration(
  "Xcode (MacOSX)"
  NAME "Debug"
  # PREPROCESSOR_DEFINITIONS
)

jucer_export_target_configuration(
  "Xcode (MacOSX)"
  NAME "Release"
  # PREPROCESSOR_DEFINITIONS
)

jucer_export_target(
  "Visual Studio 2015"
  # EXTRA_PREPROCESSOR_DEFINITIONS
  # EXTRA_COMPILER_FLAGS
)

jucer_export_target_configuration(
  "Visual Studio 2015"
  NAME "Debug"
  # PREPROCESSOR_DEFINITIONS
)

jucer_export_target_configuration(
  "Visual Studio 2015"
  NAME "Release"
  # PREPROCESSOR_DEFINITIONS
)

jucer_project_end()
